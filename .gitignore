venv
__pycache__
*.pyc
*.pyo
.DS_Store

# Secrets — never commit
.env
.env.local
edj-env.txt
deploy-secrets.txt
edj-stack.yml

# Runtime state / secrets (live on the data volume; ignore if they appear locally)
.session_secret
.admin_token_version
keys.json
keys.json.tmp

# Backups & logs
*.bak
*.log
